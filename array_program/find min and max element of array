#include<stdio.h>

int main()
{
    int a[5]={10,40,324,475,231};
    int min=a[0],max=a[0];
    for(int i=0;i<5;i++)
    {
        if(a[i]<min)
            min=a[i];
        if(a[i]>max)
            max=a[i];
        
    }
    printf("%d %d",min,max);
}
