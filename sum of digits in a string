#include<stdio.h>
#include<string.h>

int main()
{
  char str[]="232jfjsdf2328983hru4;
  int sum=0,length=strlen(str);
  int i;

  for(i=0;i<length;i++)
  {
    if((str[i]<'0' && (str[i]>'9'))
    {
        sum += (str[i] -'0');
    }

  }
  printf("%d",sum);
  return 0;
  }
